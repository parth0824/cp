#include <bits/stdc++.h>
using namespace std;
#define ll long long int
void solve()
{
    int m, n, o;
    cin >> m >> n >> o;
    if(m == n && n == o){
        cout<<-1<<"\n";
    }
    else if(m + n == o){
        
    }
}
int main()
{
    ios::sync_with_stdio(false);
    cin.tie(0);
    cout.tie(0);
    int t;
    cin >> t;
    while (t--)
        solve();
    return 0;
}